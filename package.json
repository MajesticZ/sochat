{
    "name": "chat-example",
    "version": "0.0.1",
    "description": "A chat example to showcase how to use `socket.io` with a static `express` server with `async` for control flow.",
    "main": "server.js",
    "repository": "",
    "author": "Patryk Wlazłowicz",
    "scripts": {
        "start": "node server.js",
        "debug": "node debug server.js",
        "start-db": "mongod --dbpath=database",
        "create-db": "mkdir database",
        "create-ssl-certificate": "mkdir ssl; cd ssl; openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes"
    },
    "dependencies": {
        "body-parser": "^1.17.2",
        "cookie-parser": "^1.4.3",
        "crypto": "0.0.3",
        "express": "^4.15.3",
        "moment": "^2.18.1",
        "mongoose": "^4.10.5",
        "morgan": "^1.8.2",
        "socket.io": "^2.0.2",
        "uuid": "^3.0.1"
    }
}
